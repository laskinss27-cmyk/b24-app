# /root/sync/.env — переменные синка и бэкапа. Заполнить на VPS; в git значения НЕ коммитим.
DEV_WEBHOOK=          # https://umniydom.bitrix24.ru/rest/<user>/<token>/ — для заливки бэкапа БД на Б24-Диск
ERPNEXT_URL=http://localhost:8080
ERPNEXT_TOKEN=        # token <api_key>:<api_secret>
