# Обратные статусы заказов: включение 20.09.2026

По подтверждению пользователя включены только категории сделок 0 (Объекты)
и 6 (Быстрая продажа). Закрытый качественный лид не означает выполненный заказ.

Production receiver и worker: `b24-orders:status-20260920-03`.
Endpoint: `https://201.51.12.57.sslip.io/api/integrations/umniydom/v1/orders/status`.
Секрет отдельный, хранится только на сервере. Секрет создания заказов не менялся.
Receiver: statusMode=production, worker создания: statusMode=off, оба понимают v2.
Сайт уже запустил постоянный status worker; его orders.sqlite имеет схему v4.

Исправления после совместной проверки:

- Неподтверждённое наблюдение не стирает последнюю подтверждённую CRM-связь.
- Исчезнувшая сделка не откатывает заказ к старому лиду; другая сделка не
  подменяет ранее подтверждённую при неоднозначности.
- `user.get` на рабочем вебхуке возвращал insufficient_scope. Для получения
  имени используется разрешённый `im.user.get`: проверяется ID, сохраняются
  только ID/name. Права вебхука не расширялись.
- В текущую исходную папку возвращён `maintenance.ts` из ранее выпущенного
  исходника; добавлена поддержка schema 1/2 с проверкой status-таблиц.
- `/opt/b24-orders/backup.py` проверяет восстановление точным image ID текущего
  receiver, а не закреплённым schema-1 образом от 14 сентября. Расписание прежнее.

Проверено: 35 интеграционных/maintenance-тестов, backend typecheck,
совместный реальный HTTP + временные SQLite/fake CRM (11 сценариев).
На production: 9 проверенных заказов, 4 resolved, 5 unresolved
(2 ambiguous, 3 not_found), 0 ошибок синхронизации. Неопределённые исторические
заказы не закрывались автоматически. Повторных отправок/новых CRM-записей нет.
Очередь done=9, attempts=9 до/после; сайт accepted=9, attempts=10.

Основной b24-backend и каталог не перезапускались. Internal/public health и
authenticated read-only ERPNext = 200; сеть erpnext_frappe_network сохранена.
Backup со свежим restore-check и внешней копией прошёл:
`20260920T130255Z-aacbcc14`, records=9. Monitor/backup/retention timers активны.
Штатный retention после выпуска удалил 0 контейнеров и 0 образов.

Пакет выпуска: `/opt/umniydom-site/releases/order-status-20260920-01`;
финальные модули в `im-fix/` и `maintenance-fix/`; `verification.json` содержит
агрегированные результаты. Предмиграционные online-снимки в `backups/`.
Первоначальный release.tar.gz относится к образу status-20260920-01, не финальному 03.
Для аварийного отключения сохранить совместимые бинарники и выключить status
worker/endpoint. Не возвращать старые schema-1/schema-3 образы поверх новых БД.

Отдельно выявлен рассинхрон авторитетных DNS InSales: ns3 даёт новый VPS,
ns4 старый IP. В этой задаче DNS не менялся; admin.umniydom.pro работает на VPS.

Git commit/push не выполнялись. Не перезаписывать эти исходники старым пакетом.
