# Проверка с реальным Битриксом — 14.09.2026

По прямому запросу пользователя выполнен один контролируемый тест реальных
CRM-операций. Результат: **успешно**, один лид и одно сообщение в чате.

- Лид: [44626](https://umniydom.bitrix24.ru/crm/lead/details/44626/).
- Заголовок: «Заказ UD-91789402041000 — ТЕСТ ИНТЕГРАЦИИ — НЕ ОБРАБАТЫВАТЬ».
- Ответственный: «Автозадачи», 2812.
- Стадия: NEW, «Не обработан».
- Чат: «Заказы интернет магазин», chat19572.
- Сообщение: 483938; повторным чтением подтверждены чат, тестовая маркировка
  и ссылка на созданный лид.
- Квитанция: inbox-6ecebc9f-411a-41a5-bb00-1f44b7c298cb.
- Очередь: done / complete, без причины ошибки.

Проверен путь: настоящий HTTP-запрос на локальный приёмник → постоянная SQLite
→ штатный processOne и BitrixOrdersCrm → реальные API crm.lead.add/im.message.add.
Использован код подготовленной рабочей версии. Перед тестом чтением подтверждены
доступ к чату/CRM, активность «Автозадачи» и существование стадии NEW.

Вымышленные контактные данные предварительно проверены на отсутствие совпадений
с существующими клиентами. Использовались отдельные sourceId/eventId/order.id
и отдельная локальная база. Контракт шёл в production-режиме, order.test=false,
с явными тестовыми надписями в имени, комментарии и позиции. Защита sandbox от
live-операций не менялась. Продукт ручной, цена неизвестна; оплата и резервирование
не запрашивались. Существующие клиенты/лиды не изменялись.

После завершения выполнены 20 повторных HTTP-запросов, перезапуск HTTP-приёмника
и ещё один вызов обработчика. Все повторы сохранили квитанцию; дополнительной
работы для обработчика не было. По внешнему ключу найден ровно один лид.
Журнал ограниченного запуска содержит ровно две подтверждённые попытки записи:
создание лида 44626 и отправку сообщения 483938.

Для этого разового теста явно использован существующий серверный доступ
(отправитель 1858). Вебхук не выводился, не копировался в новую конфигурацию
и не включён в отчёт. Он не назначен постоянным доступом новой интеграции.
Ответственный лида остаётся 2812 независимо от пользователя отправителя.

Основной backend, SQL-каталог, Тильда, конфигурация production и сайт не менялись.
Приёмник закрыт после проверки; постоянный worker не запускался. Публичный
HTTPS-маршрут и доставка с рабочего сайта этим тестом не проверялись.
Права отсутствующих менеджеров и все возможные отложенные автоматизации портала
тоже не проверены; на момент контрольного чтения стадия и ответственный сохранились.

Тестовые лид и сообщение оставлены для просмотра пользователем. Автоматически
удалять их или повторно запускать live-canary нельзя. При неизвестном исходе
новой попытки сначала сверить журнал, очередь и реальные CRM-объекты.

Локальные доказательства сохранены в
`outputs/umniydom-integration-20260914/live-canary-result.json`,
`live-preflight.json` и `live-canary/` (база, вымышленный заказ, журнал намерений).
Эти данные не переносятся в рабочую очередь сайта.
