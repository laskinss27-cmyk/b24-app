# Доработка: состояния части остатка

Актуальная интеграция находится в отдельной рабочей копии:
`D:/Projects/b24-app/outputs/stock-conditions/production-worktree`.

Она основана на production commit `04b9e37` и сохраняет доработку расходников. Исходная ветка этого каталога
(`0162f23`) старее production; её нельзя разворачивать поверх работающего сервера.
Первые изменения в исходной ветке оставлены как рабочие материалы, итоговая
совместимая версия и тесты находятся в указанной отдельной рабочей копии.

Подробности, ограничения и порядок выпуска:
`outputs/stock-conditions/production-worktree/docs/stock-conditions.md`.

Локально прошли сборка, проверка типов, основной backend-набор 440/440,
frontend-набор 139/139, профильные проверки 12 backend + 7 frontend и 14 проверок расходников,
браузерная проверка разделения 10 на 9 обычных + 1 сток и выбора стока при продаже.
Скриншоты: `outputs/stock-conditions/quantity-split.png`,
`outputs/stock-conditions/sale-choice.png`.

Выложено после подтверждения пользователя 2026-09-07 около 20:05 МСК: commit `7c19896`, image `b24-app:7c19896-stock-conditions-20260907170413463`.
Rollback сохранён: `b24-backend-prev-before-7c19896-stock-conditions-20260907170413463`.
Internal/public health, официальный ERP read, сеть `erpnext_frappe_network`, публичный frontend и неизменность environment проверены. 33 профильных проверки повторно прошли внутри production-образа.
Повторная проверка после обрыва 2026-09-08 10:28 МСК: оба health HTTP 200, та же версия, restart=0, чтение остатков и интерфейс исправны.
Артефакты: `outputs/stock-conditions/production-deployment.json`, `production-verification.json`, `reconnection-verification.json`.
Реальные остатки не переводились между состояниями. Пример 10 → 9 + 1 и продажа стока проверены с имитацией ERP; первый фактический перевод выполняет пользователь для выбранного экземпляра. Режимы SQL/OAuth не менялись, поля/склады состояний при запуске не создавались.
