# Экспорт инвентаризации в Excel

**Выложено 2026-09-08 по явной команде пользователя:** все три доработки, image `b24-app:6d21525-inventory-20260908`, миграция 0113. Проверены реальная MariaDB, backup/restore, 11 инвентаризаций и 11 Excel, internal/public health/ready, ERP и сеть. Подробный журнал и обязательный совместимый откат: `outputs/inventory-excel/release-worktree/docs/inventory-release-2026-09-08.md`. Последующие строки о запрете выкладки описывают прежний этап.

Реализация изолирована от незавершённых изменений основного checkout:

- Рабочая копия: `outputs/inventory-excel/release-worktree`.
- Ветка: `codex/inventory-excel-export`.
- Готовый коммит: `378eb83`.
- Основа: `7c19896`, с уже выложенными состояниями остатков и расходными материалами.
- Функция: кнопки «Скачать Excel» и «Excel склада» в разделе «Складской учёт → Инвентаризация».
- Инструкция и детали: `outputs/inventory-excel/release-worktree/docs/inventory-excel.md`.

Проверены тесты экспорта, общие тесты backend/frontend, сборка и локальный интерфейс. Production не изменён. Для выкладки требуется отдельное подтверждение пользователя, проверка актуальной версии работающего контейнера и соблюдение AGENTS.md.

Следующее изменение в той же ветке: один обязательный общий комментарий к заказу из сделки вместо комментариев строк. Подробности в `outputs/inventory-excel/release-worktree/docs/supply-order-comment.md`. Пользователь явно указал НЕ выкладывать: оба изменения остаются локальными до новой команды.

Третья доработка: денежные итоги недостачи и излишков по розничным ценам, фиксируемым при отправке отчёта. Добавлены в интерфейс и Excel, коммит `77a0663`. Подробности, проверки и условия миграции/отката — в `outputs/inventory-excel/release-worktree/docs/inventory-money.md`. Комментарии к заказам: коммит `ebbcd2f`. Все три изменения НЕ выложены. Миграция 0113 подготовлена, но не запускалась; до выкладки требуется её проверка на тестовой MariaDB и план отката с учётом новых state hash.
